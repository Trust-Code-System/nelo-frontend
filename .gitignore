# dependencies
node_modules

# build output
.next
out
*.tsbuildinfo

# environment — never commit real Channel tokens or API origins
.env
.env*.local

# NOTE: src/lib/vendure/generated is deliberately NOT ignored. It is committed so CI can
# diff a fresh codegen run against it and fail on schema drift. Never hand-edit it.

# visual-gate renders: ~14MB of @2x PNGs, regenerable from design/mockups
design/shots

.DS_Store

# playwright
test-results
playwright-report
blob-report
.playwright
