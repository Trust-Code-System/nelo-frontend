# SUPERSEDED.
#
# This proposal is no longer the Atelier Shop API contract. The backend (nelo-commerce,
# PR #30, "feat(atelier): expose measurement profiles and appointments to customers and
# staff") implemented a real contract that differs from this draft in several ways - one
# display unit per profile, non-null profile measurements, no `rescheduled` status, paginated
# appointment lists, and the removal of fees, `alternateAt`, `atelierAvailability` and
# commission-request operations, among others.
#
# The real, authoritative contract is nelo-commerce's
# docs/plans/2026-09-24-atelier-shop-api-design.md, under "Shop API contract". This
# storefront's operations against it live in src/lib/vendure/operations/atelier.graphql, and
# the committed schema is schema/shop-api.graphql.
#
# Do not code against this file. It is kept only so its history remains attached to the repo.
